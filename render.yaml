services:
  - type: web
    name: fylo-backend
    env: docker
    dockerfilePath: ./backend/Dockerfile
    disk:
      name: uploads
      mountPath: /data/uploads
      sizeGB: 1

  - type: web
    name: fylo-frontend
    env: docker
    dockerfilePath: ./frontend/Dockerfile
